# 语音流推送对接简版文档

适用于第三方公司向我方系统实时推送通话语音流或转写结果。

---

## 1. 对接目标

第三方公司作为独立上游系统，负责向我方系统实时推送通话语音流或转写结果。

我方系统接收后进行语音识别、文本分析和业务处理。

本文件仅明确传输协议、消息格式、音频格式和最小验收要求，不涉及业务算法和内部系统实现。

---

## 2. 传输方式

推荐使用 **WebSocket 长连接**，一通电话一个连接。

```text
WSS wss://sqasr.telewave.com.cn:8443/asr?callId=<callId>
```

生产环境必须使用 TLS；每条连接必须携带鉴权信息。

---

## 3. 基本时序

```text
connect
  -> call.started
  -> audio.frame 或 speech.final
  -> heartbeat
  -> call.ended
disconnect
```

---

## 4. 统一消息格式

所有事件统一使用 JSON 格式，外层字段保持一致，具体内容放在 `payload` 中。

```json
{
  "schemaVersion": "1.0",
  "eventId": "evt-000001",
  "eventType": "audio.frame",
  "callId": "call-20260506-0001",
  "streamId": "stream-main",
  "seq": 1,
  "timestampMs": 320,
  "sendTimeMs": 1778040000320,
  "sourceSystem": "upstream-company",
  "payload": {}
}
```

| 字段 | 是否必填 | 说明 |
|---|---|---|
| `schemaVersion` | 是 | 协议版本，固定为 `1.0` |
| `eventId` | 是 | 事件唯一 ID，用于去重 |
| `eventType` | 是 | 事件类型 |
| `callId` | 是 | 通话唯一 ID |
| `streamId` | 是 | 流 ID |
| `seq` | 是 | 同一通话内递增序号 |
| `timestampMs` | 是 | 相对通话开始的毫秒时间 |
| `sendTimeMs` | 建议 | 上游发送时间，Unix 毫秒 |
| `sourceSystem` | 建议 | 上游系统名称 |
| `payload` | 是 | 事件具体内容 |

---

## 5. 主要事件类型

| 事件类型 | 说明 | 是否必须 |
|---|---|---|
| `call.started` | 通话开始，必须先于语音数据发送 | 是 |
| `audio.frame` | 原始音频帧，适用于上游推送语音流 | 二选一 |
| `speech.final` | 稳定转写文本，适用于上游已完成 ASR | 二选一 |
| `heartbeat` | 心跳包，建议每 10 秒一次 | 是 |
| `call.ended` | 通话结束 | 是 |

---

## 6. 原始音频推送格式

如果上游推送原始语音流，使用 `audio.frame`。

```json
{
  "eventType": "audio.frame",
  "callId": "call-20260506-0001",
  "seq": 10,
  "timestampMs": 320,
  "payload": {
    "speaker": "caller",
    "direction": "inbound",
    "startTimeMs": 320,
    "endTimeMs": 640,
    "codec": "pcm_s16le",
    "sampleRate": 16000,
    "channels": 1,
    "frameDurationMs": 320,
    "audioBase64": "..."
  }
}
```

| 项目 | 要求 |
|---|---|
| 优先音频格式 | `pcm_s16le`，16kHz，mono |
| 兼容音频格式 | `pcm_s16le`，8kHz，mono；`opus`，16kHz，mono |
| 音频帧长度 | 建议 20ms - 320ms |
| 音频内容 | 使用 Base64 放入 `audioBase64` |
| 双声道要求 | 必须说明左 / 右声道分别对应 `caller` 或 `agent` |

---

## 7. 转写文本推送格式

如果上游已完成语音识别，可直接推送 `speech.final`。

```json
{
  "eventType": "speech.final",
  "callId": "call-20260506-0001",
  "seq": 121,
  "timestampMs": 22320,
  "payload": {
    "segmentId": "seg-0008",
    "speaker": "caller",
    "direction": "inbound",
    "startTimeMs": 19640,
    "endTimeMs": 22320,
    "text": "深圳大学南区三栋二单元五楼有人被困",
    "confidence": 0.91,
    "language": "zh-CN"
  }
}
```

---

## 8. 说话人和方向要求

| 字段 | 可选值 | 说明 |
|---|---|---|
| `speaker` | `caller` | 来电人 / 报警人 |
| `speaker` | `agent` | 接警员 / 坐席 |
| `speaker` | `system` | 系统播报 |
| `speaker` | `unknown` | 未知说话人 |
| `direction` | `inbound` | 来电方向 |
| `direction` | `outbound` | 去电方向 |
| `direction` | `mixed` | 混合或重叠说话 |
| `direction` | `unknown` | 未知方向 |

要求：

- 至少能区分 `caller` 和 `agent`
- 不得把来电人与接警员内容合并到同一段音频或同一段文本中

---

## 9. 顺序、重传和心跳要求

- `seq` 必须在同一 `callId` 内单调递增。
- `eventId` 必须唯一。
- 重传同一事件时，`eventId` 和 `payload` 必须保持一致。
- `timestampMs`、`startTimeMs`、`endTimeMs` 必须使用毫秒级时间。
- `endTimeMs` 必须大于或等于 `startTimeMs`。
- 断线重连后，应从最后确认的 `seq` 之后继续发送。
- `heartbeat` 建议每 10 秒发送一次。
- 30 秒无事件或心跳可视为连接异常。

---

## 10. ACK 返回格式

```json
{
  "type": "ack",
  "callId": "call-20260506-0001",
  "receivedSeq": 121,
  "accepted": true,
  "message": "ok"
}
```

| 错误码 | 说明 |
|---|---|
| `INVALID_JSON` | JSON 解析失败 |
| `MISSING_REQUIRED_FIELD` | 缺少必填字段 |
| `DUPLICATE_EVENT` | 重复事件 |
| `SEQ_GAP` | 序号跳号 |
| `INVALID_SPEAKER` | `speaker` 非法 |
| `TIMESTAMP_REGRESSION` | 时间戳倒退 |
| `PAYLOAD_TOO_LARGE` | 单条消息过大 |

---

## 11. 联调验收清单

- 可以稳定发送 `call.started` 和 `call.ended`
- 每条事件都包含 `callId`、`eventId`、`seq`、`timestampMs`
- 音频或文本片段包含 `startTimeMs` 和 `endTimeMs`
- `speaker` 至少能区分 `caller` 和 `agent`
- 音频格式符合 `pcm_s16le / 16kHz / mono` 优先要求
- 支持 `heartbeat`、ACK 和断线重连
- 联调前提供不少于 10 条完整 JSONL 样本

---

## 12. 样本文件建议

```text
samples/
├── normal_call.jsonl
├── fire_call.jsonl
├── noisy_call.jsonl
├── caller_interrupts_agent.jsonl
└── out_of_order_events.jsonl
```

每个样本应包含完整生命周期：

```text
call.started
audio.frame 或 speech.final
heartbeat
call.ended
```
