# ASR 五组件技术说明

## 1. 整体技术链路

```text
语音流处理 → VAD → 动态热词加载 → ASR 转写 → ASR 后处理
```

## 2. 组件说明

| 组件 | 输入 | 输出 | 实现技术 | 功能描述 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 语音流处理 | 语音流 | 语音流 | WebSocket Bridge 协议、Paraformer模型、科大讯飞方言模型API | 切换语音模型；保持；转接；三方通话；接收 CTI 双路音频，区分报警人和接警人；检查断帧和音量过低；将音频送入 VAD 和 ASR； | 保持期间按 `callId` 丢弃音频；转接时保留历史记录并绑定新坐席；三方通话需要增加第三路 `participantId` |
| VAD | 双路语音流 | 切分后的语音流 | `webrtcvad`、能量门限、20ms 分帧、连续有声确认、静音超时、300ms 前置缓存、滑动窗口 | 过滤静音和噪声；检测每一路说话开始、结束；控制有效音频进入 ASR；结束时触发 ASR final | 当前参数约为连续 4 帧确认开始、1300ms 静音确认结束、最短语音 500ms，均可通过环境变量调整。单路 `speech.end` 切 ASR 语句；双方事件组合切问答轮次 |
| 动态热词加载 | 基站定位、当前问询阶段、场景分类结果、热词文件 | 当前热词阶段 | PostGIS、场景状态机、本地 TXT 热词库、文件缓存和去重 | 根据地址询问、火灾基础问询、高层、人员密集场所、危化品、电梯等场景加载对应热词，提高专业词和地址词识别概率 | 热词切换通常在下一次 VAD 语句开始、建立新 FunASR 握手时生效，不建议在一句话中途切换 |
| ASR 转写 | 语音流 | 初始文本 | FunASR WebSocket、Paraformer、科大讯飞方言模型API | 把有效语音实时转成文字；流式结果用于前端实时展示，离线结果作为最终文本进入后处理 | caller/agent 应独立维护识别上下文。后续接入讯飞方言 API 时，可增加统一 `AsrEngine` 接口和模型路由器 |
| ASR 后处理 | ASR 初始文本 | 纠正后文本、高亮关键词  | 规则：地址库表达分、拼音编辑相似度、地址库命中分、热词/词频权重、上下文槽位一致性分、低置信度候选惩罚 | 识别地址片段；利用拼音、别名和地址库生成候选；执行地址纠错；提取地址、灾情、楼栋等关键词；低置信度时保留原文 | 当前代码已过滤“一个/某个/这个/那个小区、地方、路口”等泛指表达；“我在、位于、地址是”等位置引导词加分仍属于待补充规则。当前地址库查询主要使用 PostgreSQL 字段，尚未执行 PostGIS 空间计算 |


